import React from 'react';
import NavLinks from './NavLinks';

const Navbar = () => (
  <nav>
    <div>NavBar Component</div>
    <div>
      <NavLinks />
    </div>
  </nav>
);

export default Navbar;
